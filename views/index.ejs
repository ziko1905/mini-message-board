<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
</head>
<body>
    <h1>Messages: <a href="/new">Create new</a></h1>
    <% messages.forEach((msg, index) => { %>
    <%- include('message', {user: msg.username, message: msg.text, date: msg.added, index}) %>
    <% }) %>
</body>
</html>